#!/usr/bin/env ruby

#
# `until` means “do something until the test condition becomes true.
# In other words, keep going while the condition is false.
#
# `until` is the opposite of `while`.
#

counter = 0
until counter > 10
    puts counter
    counter += 1
end


