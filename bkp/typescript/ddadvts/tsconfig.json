{
  "compilerOptions": {
    /* Set the JavaScript language version for emitted JavaScript and include
    * compatible library declarations. */
    "target": "ES2017",

    /* Specify what module code is generated. */
    "module": "commonjs",

    /* Emit additional JavaScript to ease support for importing CommonJS
     * modules.  This enables `allowSyntheticDefaultImports` for type
     * compatibility. */
    "esModuleInterop": true,

    /* Ensure that casing is correct in imports. */
    "forceConsistentCasingInFileNames": true,

    /* Enable all strict type-checking options. */
    "strict": true,

    /* Skip type checking all .d.ts files. */
    "skipLibCheck": true,

    /* When doing studies or playing with stuff, I don't want to be
     * bothered with “unused” variables. Of course that should be
     * set to true for real, serious projects.
     *
     * NOTE: Setting these to false causes them not to be threated as
     * errors, but you still see them grayed out and if you hover the
     * warning (not error) about the variable not being use will still
     * be present. */
    "noUnusedLocals": false,
    "noUnusedParameters": false,
  }
}
